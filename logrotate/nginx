/var/log/nginx/*.log {
    weekly
    rotate 12
    missingok
    notifempty
    compress
    delaycompress
    sharedscripts
    create 0640 nginx nginx
    postrotate
        /etc/init.d/nginx reload > /dev/null 2>&1 || true
    endscript
}
